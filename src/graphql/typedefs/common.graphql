# Interface for sequelize common fields
interface Timestamps {
  created: Date!
  updated: Date!
}

# Interface for errors
type Error {
  message: String!
}

type NotAuthenticatedError {
  message: String!
}
