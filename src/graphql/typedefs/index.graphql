# Custom Scalars
scalar Date

type Query {
  # User
  currentUser: User
}

type Mutation {
  # User
  signUp(name: String!, email: String!, password: String!): User
  verifyUser(email: String!, password: String!, verificationCode: String): User
  login(email: String!, password: String!): Login
}
